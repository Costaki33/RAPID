# Core deps already expected in the eqcctpro env:
#   torch, obspy, numpy, matplotlib, ray, seisbench
#
# Optional, for the ONNX / TensorRT backends:
onnx>=1.16
onnxruntime-gpu>=1.18   # use onnxruntime (CPU-only) if no CUDA
# TensorRT wheels are distributed separately by NVIDIA for each CUDA toolkit;
# install the wheel matching your CUDA version, then:
#   pip install pycuda
# For CUDA 12.x:
#   pip install tensorrt
#   pip install pycuda
