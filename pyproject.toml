[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "rapid"
version = "1.0.0"
description = "RAPID: real-time deep-learning seismic phase picking with Slipstream inference and Model-Actor / Ripper orchestration"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Constantinos Skevofilax", email = "cskevofilax@utexas.edu" },
]
keywords = [
    "seismology",
    "phase-picking",
    "seisbench",
    "deep-learning",
    "ray",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy>=1.26",
    "scipy>=1.10",
    "pandas>=2.0",
    "matplotlib>=3.7",
    "obspy>=1.4",
    "seisbench>=0.10",
    "torch>=2.2",
    "psutil>=5.9",
    "nvidia-ml-py>=12.0",
]

[project.optional-dependencies]
# Ray-based orchestration (Model-Actor / Ripper). Install for network-scale picks.
orchestration = ["ray>=2.40"]

[project.urls]
Homepage = "https://github.com/ut-beg-texnet/eqcct"
Repository = "https://github.com/ut-beg-texnet/eqcct"

[tool.setuptools.packages.find]
include = ["rapid*"]
